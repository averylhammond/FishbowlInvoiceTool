name: Release Workflow

# Build and publish a release whenever a version tag (e.g. v3.2.0) is pushed.
on:
  push:
    tags: [ 'v*' ]

# Allow the job to create the GitHub Release and upload the packaged asset
permissions:
  contents: write

jobs:
  release:

    # Give the job a name to link into the Github Rules UI
    name: Build and Publish Release
    runs-on: windows-latest

    # Make sure that windows is using bash shell, since all terminal commands in this job are bash style
    defaults:
      run:
        shell: bash

    steps:
    # Checkout repo, including the private submodule that supplies packaging resources
    - name: Checkout repo
      uses: actions/checkout@v4
      with:
        submodules: recursive
        token: ${{ secrets.CUSTOMER_DATA_PAT }}

    # Set up Python environment
    - name: Set up Python environment
      uses: actions/setup-python@v4
      with:
        python-version: "3.11.9"

    # Install project dependencies (dev set, needed to run the tests below)
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements/dev.txt

    # Fail the release if the pushed tag does not match the in-app version,
    # so a release can never ship with an About box that disagrees with the tag
    - name: Verify tag matches source version
      run: |
        TAG="${GITHUB_REF_NAME#v}"
        VERSION="$(python -c 'from source import constants; print(constants.VERSION)')"
        echo "Tag version: $TAG / constants.py VERSION: $VERSION"
        if [[ "$TAG" != "$VERSION" ]]; then
            echo "::error::Tag ($TAG) does not match source/constants.py VERSION ($VERSION)."
            exit 1
        fi

    # Fail the release if the patch notes say nothing about the version being released.
    # The app shows these notes on the first launch after an update, so a release missing
    # its section would ship silently and only surface when a customer updated into it.
    - name: Verify patch notes document this version
      run: |
        VERSION="$(python -c 'from source import constants; print(constants.VERSION)')"
        if ! grep -qE "^## v?${VERSION}( |$)" PATCH_NOTES.md; then
            echo "::error::PATCH_NOTES.md has no '## ${VERSION}' section."
            exit 1
        fi

    # Copy integration test data into the expected input directory
    - name: Copy integration test data
      run: |
        ./scripts/copy_resources.sh

    # Run unit tests against the tagged commit
    - name: Run unit tests
      run: |
        pytest tests/

    # Run integration tests and verify output matches the canonical results
    - name: Run integration test
      run: |
        python main.py --integration-test
        cp ./automated-invoice-testing/canonical_correct_results.txt .
        if diff -q ./logs/results.txt canonical_correct_results.txt >/dev/null; then
            echo -e "\e[1;3;32mOutput matches, tests pass.\e[0m"
        else
            echo -e "\e[1;3;31mOutput mismatch, tests do not pass.\e[0m"
            exit 1
        fi

    # Install Inno Setup so package_release.sh can additionally build the Windows installer
    # (FishbowlInvoiceTool_Setup.exe). Chocolatey is preinstalled on windows-latest and installs
    # ISCC.exe to the default location the packaging script probes.
    - name: Install Inno Setup
      run: |
        choco install innosetup --no-progress -y

    # Package the release executable + resources into release/FishbowlInvoiceTool.zip, and (since
    # Inno Setup is now installed) release/FishbowlInvoiceTool_Setup.exe
    - name: Package release
      run: |
        ./scripts/package_release.sh false

    # Publish a checksum for each artifact so the in-app updater can verify the
    # installer it downloads before running it. Written from inside release/ so the
    # file names in it are bare, matching the asset names on the GitHub Release.
    - name: Write release checksums
      run: |
        cd release
        sha256sum FishbowlInvoiceTool.zip FishbowlInvoiceTool_Setup.exe > SHA256SUMS.txt
        cat SHA256SUMS.txt

    # Create the GitHub Release with the packaged zip, installer, checksums and
    # auto-generated notes
    - name: Create GitHub Release
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        gh release create "$GITHUB_REF_NAME" \
          release/FishbowlInvoiceTool.zip \
          release/FishbowlInvoiceTool_Setup.exe \
          release/SHA256SUMS.txt \
          --title "$GITHUB_REF_NAME" \
          --generate-notes
